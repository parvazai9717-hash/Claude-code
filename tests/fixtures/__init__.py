"""Shared test fixture data.

Any credential-shaped string used anywhere in this suite is synthetic and exists
solely so a test can assert that redaction removes it. No real credential is ever
committed, and the suite never reads one from the environment.
"""
