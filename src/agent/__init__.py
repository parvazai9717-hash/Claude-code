"""local-agent: a local-first, provider-independent autonomous agent runtime.

The model is the brain, the runtime is the orchestration layer, tools are the
hands, and the workspace is the computer. Every capability is enforced in code by
the security layer rather than by instructions to the model.
"""

from __future__ import annotations

__version__ = "0.1.0"

__all__ = ["__version__"]
